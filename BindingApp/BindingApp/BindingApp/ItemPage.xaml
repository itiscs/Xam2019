<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="BindingApp.ItemPage">
    <ContentPage.Content>
        <StackLayout>
            <Frame BackgroundColor="#2196F3" Padding="24" CornerRadius="0">
                <Label Text="Welcome to Xamarin.Forms!" HorizontalTextAlignment="Center" TextColor="White" FontSize="36"/>
            </Frame>

            <StackLayout Orientation="Horizontal">
                <Image Source="{Binding ImagePath}" WidthRequest="200" HeightRequest="200" />
                <StackLayout>
                    <Label Text="{Binding Title}" FontSize="24" />
                    <Label Text="{Binding Company}" FontSize="24" />
                    <Label Text="{Binding Price}" FontSize="24" />
                </StackLayout>
            </StackLayout>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>